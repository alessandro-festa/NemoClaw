# Automated configuration
We assume that during nemoclaw onboarding process will have a way to connect to the SUSE aif-nc (let's call it "SUSE AI Factory with NVIDIA") operator and retrieve the configuration. To do so we'll input an apikey provided to the user and an url (either in the form of https://{IP}:{PORT} or https://{host}.{domain}:{port}) to retrieve the right configuration and bypass the interactive onboarding process.

use @/Users/alessandrofesta/Documents/dev/aif-nc/.claude/rules/rancher-ui.md to understand the server-side flow
